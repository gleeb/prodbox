# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

HEAD    /api/track/count          com.fourstars.prodbox.controllers.TrackingController.count()
POST    /api/track/:url           com.fourstars.prodbox.controllers.TrackingController.trackNewUrl(url:String)
